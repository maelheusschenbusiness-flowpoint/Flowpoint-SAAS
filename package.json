{
  "name": "flowpoint-saas",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "pnpm -r run build",
    "dev": "pnpm --filter @workspace/api-server run dev"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}
